// Generated by rd install pi — do not edit by hand.
// Re-run `rd install pi` to update.
import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";
import { execFileSync } from "node:child_process";

function rd(...args: string[]): void {
  try {
    execFileSync("rd", args, { timeout: 5000, stdio: "ignore" });
  } catch {
    // Silently ignore errors (e.g. rd not in PATH, outside tmux)
  }
}

export default function (pi: ExtensionAPI) {
  pi.on("session_start", async () => {
    rd("agent", "status", "idle", "--harness", "pi");
  });

  pi.on("before_agent_start", async () => {
    rd("agent", "status", "working", "--harness", "pi");
  });

  pi.on("agent_settled", async () => {
    rd("agent", "status", "done", "--harness", "pi");
  });

  pi.on("session_shutdown", async () => {
    rd("agent", "clear");
  });
}
