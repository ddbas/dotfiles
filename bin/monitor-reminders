#!/usr/bin/env bash

monitor \
  -b "return:execute(echo {} | awk '{print \$1}' | xargs remind complete)+reload(remind list)+clear-query" \
  -b "ctrl-m:execute(read 'reminder?remind me> '; [[ -n \"\$reminder\" ]] && remind me \$reminder)+reload(remind list)+clear-query" \
  "remind list"
