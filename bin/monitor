#!/usr/bin/env bash

CMD=$@

reload_loop() {
    while true;
    do
        curl -XPOST localhost:6266 -d "reload($CMD)"
        sleep 5
    done
}

reload_loop &> /dev/null &
reload_loop_job=$!

$CMD | fzf --listen 6266

kill $reload_loop_job
