#!/usr/bin/env bash

if ! tmux has-session -t=reminders 2> /dev/null; then
    tmux new-session -d -s reminders
    tmux send-keys -t reminders:0 "remind list -i" Enter
fi

tmux switch-client -t reminders
